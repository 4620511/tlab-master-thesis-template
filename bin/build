#!/usr/bin/env bash

DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" &>/dev/null && pwd)

docker-compose -f "$DIR"/../.devcontainer/docker-compose.yml run latex latexmk
docker-compose -f "$DIR"/../.devcontainer/docker-compose.yml down
"$DIR"/clean
