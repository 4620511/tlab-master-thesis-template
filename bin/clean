#!/usr/bin/env bash

set -eu

DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" &>/dev/null && pwd)

cd "$DIR/.."
rm -f ./*.aux ./*.bak ./*.bbl ./*.blg ./*.dvi ./*.fdb_latexmk ./*.fls ./*.lof ./*.log ./*.lot ./*.toc
